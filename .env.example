# =============================================================================
# Animus Engine — Environment Configuration
# =============================================================================
# Copy this file to .env and customize as needed.
# Most settings have sensible defaults; only ANTHROPIC_API_KEY is required.

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------

# Runtime environment: "development" or "production"
NODE_ENV=development

# Port the backend listens on
PORT=3000

# Bind address (use 0.0.0.0 for Docker/remote access)
HOST=localhost

# Log level: debug | info | warn | error
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Data Directory
# -----------------------------------------------------------------------------

# Where databases, models, logs, and media are stored.
# Defaults to ./data relative to the backend package.
# In Docker this is set to /app/data (mapped to a volume).
# ANIMUS_DATA_DIR=./data

# -----------------------------------------------------------------------------
# API Keys
# -----------------------------------------------------------------------------

# Anthropic API key (required for Claude agent, the default provider).
# Can also be configured via the Settings UI after first launch.
# ANTHROPIC_API_KEY=

# OpenAI API key (optional, only needed if using the Codex agent provider).
# OPENAI_API_KEY=

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------

# How long login sessions last before requiring re-authentication (days).
# SESSION_EXPIRY_DAYS=30

# -----------------------------------------------------------------------------
# Telemetry
# -----------------------------------------------------------------------------

# Anonymous usage telemetry is enabled by default to help improve Animus.
# No personal data, message content, or identifying information is collected.
# Set either of these to 1 to disable telemetry (can also be toggled in Settings UI).
# DO_NOT_TRACK=1
# ANIMUS_TELEMETRY_DISABLED=1

# Set to 1 to log telemetry events to the console instead of sending them.
# Useful for verifying what would be sent without actually sending data.
# ANIMUS_TELEMETRY_DEBUG=1

# -----------------------------------------------------------------------------
# Vault / Encryption
# -----------------------------------------------------------------------------

# Password to auto-unlock the encryption vault on server start.
# ANIMUS_UNLOCK_PASSWORD=

