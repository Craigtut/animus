{
  "private": true,
  "scripts": {
    "build": "esbuild src/generate-image.ts --bundle --platform=node --target=node20 --format=cjs --outfile=generate-image.js"
  },
  "dependencies": {
    "@google/genai": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
