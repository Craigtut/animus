{
  "fields": [
    {
      "key": "HA_URL",
      "label": "Home Assistant URL",
      "type": "text",
      "required": true,
      "helpText": "Base URL of your Home Assistant instance (e.g. http://homeassistant.local:8123)"
    },
    {
      "key": "HA_ACCESS_TOKEN",
      "label": "Long-Lived Access Token",
      "type": "secret",
      "required": true,
      "helpText": "Create one at Settings > User > Long-Lived Access Tokens in Home Assistant"
    }
  ]
}
